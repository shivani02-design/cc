#include<stdio.h>#include<stdlib.h>
#defineMAX_N10intn;
intboard[MAX_N][MAX_N];
intis_valid(introw,intcol){inti,j;
for(i=0;i<n;i++){
if(board[row][i]||board[i][col])return0;
}
for(i=row,j=col;i>=0&&j>=0;i--,j--){if(board[i][j])
return0;
}
for(i=row,j=col;i<n&&j>=0; i++,j--){if(board[i][j])
return0;
}
return1;
}
intsolve(intcol){introw,i;
if(col>=n)return1;
for(row=0;row<n;row++){if(is_valid(row,col)){
board[row][col]=1;if(solve(col+1))
return1;
board[row][col]=0;
}
}
return0;
}
intmain(){inti,j;
scanf("%d"
,&n);
if(solve(0)){
for(i=0;i<n;i++){for(j=0;j<n;j++){
printf("%d"
,board[i][j]);
}
printf("\n");
}
}else{
printf("Notpossible\n");
}
return0;
}
OUTPUT:
SAMPLEINPUT4
